{
  "name": "justiceos-project",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run install:all && npm run build:frontend",
    "start": "cd backend && npm start"
  }
}
